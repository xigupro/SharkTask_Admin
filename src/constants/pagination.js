/**
 * 翻页的常量定义
 */
export default {
  // 可选的分页条数列表
  pageSizes: [8, 20, 30, 40],
  // 控制要加载的组件项
  layout: 'prev, pager, next, sizes, jumper, total', //
  // 简单版的组件项
  simpleLayout: 'prev, pager, next',
  // 默认单页条数
  defaultPageSize: 8,
  // 默认页起点
  defaultPageNo: 1,
};
